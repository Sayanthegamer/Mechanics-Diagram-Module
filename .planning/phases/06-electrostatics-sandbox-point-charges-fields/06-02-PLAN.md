---
phase: 6
plan: 2
type: plan
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/lib/diagrams/EmDiagram.ts
autonomous:
  - src/lib/diagrams/EmDiagram.ts
requirements:
  - EM-02
  - EM-03
  - EM-04
must_haves:
  - Render an electric field vector grid with 30px spacing and non-linear (arctan/log) arrow scaling.
  - Draw electric field lines starting from positive charges and ending on negative charges or going off-screen, using RK2 integration with 8 lines per unit charge.
  - Render smooth equipotential contours at fixed intervals using thin semi-transparent teal/green contours.
---

# 06-02-PLAN: Electrostatics Sandbox Physics & Renderer

This plan implements the primary electrostatics physics engine and rendering algorithms in `EmDiagram.ts`.

<objective>
Implement electric field vector grids, RK2 field line path integration, and equipotential potential contour lines rendering on the canvas.
</objective>

<execution_context>
The physics coordinates map to screen coordinates using `PhysicsCanvas.toScreen` and `PhysicsCanvas.toWorld`.
</execution_context>

<context>
With point charge mechanics and interaction controls in place (from Plan 1), we now need to compute and visualize the continuous electromagnetic fields they produce.
</context>

<tasks>
  <task type="build" name="Electric Field Vector Grid">
    <files>
      <file>src/lib/diagrams/EmDiagram.ts</file>
    </files>
    <action>
      Implement drawing of the field vector grid.
      Loop over the screen with a spacing of 30px.
      Convert coordinates to physics space, calculate the electric field vector E using `getFieldAt(x, y)`.
      Scale arrow length using a clamped arctan mapping so arrows close to charges do not blow up.
      Fade arrow opacity in very weak field regions.
      Command `pc.drawArrow` to draw arrows on the canvas.
      (D-04, D-05, D-06)
    </action>
    <verify>
      Verify vector arrows are drawn in a clean grid. Arrows should point away from positive charges, toward negative charges, and scale appropriately in size and opacity without overlap or clutter.
    </verify>
    <done>false</done>
  </task>

  <task type="build" name="RK2 Field Line Path Integration">
    <files>
      <file>src/lib/diagrams/EmDiagram.ts</file>
    </files>
    <action>
      For each charge, calculate the number of lines to draw (exactly 8 lines per unit charge, based on absolute charge magnitude).
      For positive charges, start tracing at angles distributed evenly around the charge boundary.
      Integrate field line paths using RK2 stepping along the normalized field direction.
      Stop tracing when entering a negative charge's boundary, going off-screen, or exceeding a step limit.
      Draw the field lines on the canvas using bezier or linear line segments.
      (D-07, D-08, D-09)
    </action>
    <verify>
      Verify that field lines emerge from positive charges and land on negative charges or flow outwards. Check that doubling charge magnitude doubles the line density.
    </verify>
    <done>false</done>
  </task>

  <task type="build" name="Equipotential Isolines Rendering">
    <files>
      <file>src/lib/diagrams/EmDiagram.ts</file>
    </files>
    <action>
      Evaluate electric potential V across a grid representing the canvas viewport.
      Compute and render contours representing constant electric potentials (e.g., ±10V, ±20V, ±50V).
      Use a marching-squares or grid-based linear interpolation technique to trace smooth isolines.
      Render these lines on the canvas using thin, semi-transparent teal/green strokes.
      (D-10, D-11)
    </action>
    <verify>
      Verify that circular or smooth closed contour lines encircle point charges and follow standard equipotential shapes for dipoles and quadrupoles.
    </verify>
    <done>false</done>
  </task>
</tasks>

<threat_model>
- **Infinite Loop / Performance Drag**: Ensure that RK2 path integration has a strict limit on step counts (e.g., max 300 steps) and stops if field strength drops near zero (saddle points) to prevent page freeze.
- **Singularity Avoidance**: Prevent dividing by zero when checking potential or field values directly on a charge center by enforcing the softening factor `epsilonSq`.
</threat_model>

<verification>
- Verify that vector grid scale behaves predictably when charge values are adjusted.
- Inspect field line routing to confirm smooth paths matching theoretical physics layouts.
- Verify equipotential isolines remain stable and high-performance during charge dragging.
</verification>

<success_criteria>
- Clear electric field vector grid displayed across the workspace.
- Seamlessly traced RK2 electric field lines connecting positive and negative charges.
- Accurate, smooth, non-intrusive green/teal equipotential contour lines showing voltage heights.
</success_criteria>

<output>
Completed 06-02-PLAN file specifying physics and rendering components.
</output>
